# إعداد EmailJS - دليل التكوين

## 🎯 الهدف
تم تحويل نظام التوثيق من Firebase Auth إلى EmailJS لتحكم أفضل في رسائل البريد الإلكتروني.

---

## 📋 الخطوات المطلوبة

### 1️⃣ إنشاء حساب EmailJS
1. اذهب إلى: https://dashboard.emailjs.com/admin
2. سجّل حساب جديد أو سجل دخول
3. ستحصل على **Public Key** من لوحة التحكم

### 2️⃣ إضافة خدمة البريد الإلكتروني
1. في لوحة التحكم، اذهب إلى **Email Services**
2. اضغط **Add New Service**
3. اختر مزود البريد (Gmail, Outlook, إلخ)
4. اتبع التعليمات لربط بريدك
5. احفظ **Service ID**

### 3️⃣ إنشاء قالب التحقق
1. اذهب إلى **Email Templates**
2. اضغط **Create New Template**
3. أضف القالب التالي:

```html
مرحباً {{to_name}}،

رمز التحقق الخاص بك في تطبيق {{app_name}} هو:

{{verification_code}}

هذا الرمز صالح لمدة 10 دقائق فقط.

إذا لم تطلب هذا الرمز، يمكنك تجاهل هذه الرسالة.

---
فريق أثر
```

4. احفظ القالب واحفظ **Template ID**

### 4️⃣ إنشاء قالب إعادة تعيين كلمة المرور (اختياري)
1. أنشئ قالب جديد بالمحتوى:

```html
مرحباً {{to_name}}،

تلقينا طلباً لإعادة تعيين كلمة المرور في تطبيق {{app_name}}.

اضغط على الرابط التالي لإعادة تعيين كلمة المرور:
{{reset_link}}

إذا لم تطلب هذا، يمكنك تجاهل هذه الرسالة.

---
فريق أثر
```

2. احفظ **Template ID** للقالب الجديد

### 5️⃣ تحديث الكود
افتح ملف `lib/services/emailjs_service.dart` وحدّث:

```dart
// معلومات EmailJS من dashboard.emailjs.com
static const String serviceId = 'YOUR_SERVICE_ID';      // ضع Service ID هنا
static const String templateId = 'YOUR_TEMPLATE_ID';     // ضع Template ID هنا
static const String publicKey = 'YOUR_PUBLIC_KEY';       // ضع Public Key هنا
```

للقالب الثاني (إعادة تعيين كلمة المرور):
```dart
'YOUR_PASSWORD_RESET_TEMPLATE_ID'  // في السطر 119
```

---

## 🔥 إعداد Firestore Rules

أضف هذه القاعدة في `firestore.rules`:

```javascript
// السماح بإدارة رموز التحقق
match /verification_codes/{email} {
  allow read, write: if request.auth != null;
  allow create: if true; // السماح بالإنشاء للجميع
}
```

---

## ✅ التحقق من التثبيت

بعد إكمال الخطوات:

1. شغّل التطبيق:
```bash
flutter pub get
flutter run
```

2. جرب التسجيل بحساب جديد
3. تأكد من وصول رمز التحقق لبريدك
4. جرب إدخال الرمز والتحقق منه

---

## 🎨 المزايا الجديدة

✨ **واجهة احترافية:** شاشة إدخال الرمز بتصميم أنيق  
🔒 **أمان أفضل:** رموز منتهية الصلاحية (10 دقائق)  
📧 **تحكم كامل:** تخصيص رسائل البريد من لوحة EmailJS  
⚡ **سرعة:** نظام خفيف وسريع  
📱 **تجربة أفضل:** إدخال سهل للرموز المكونة من 6 أرقام

---

## ⚠️ ملاحظات مهمة

- رموز التحقق صالحة لمدة **10 دقائق** فقط
- يمكن إعادة إرسال الرمز بعد **60 ثانية**
- الرموز تُستخدم مرة واحدة فقط
- تأكد من صحة معلومات EmailJS في الكود

---

## 🆘 حل المشاكل

### الرمز لا يصل للبريد؟
- تأكد من صحة Service ID و Template ID
- تحقق من البريد المزعج (Spam)
- راجع quota في لوحة EmailJS

### خطأ في التحقق؟
- تأكد من كتابة الرمز بشكل صحيح
- تأكد من عدم انتهاء صلاحية الرمز (10 دقائق)
- جرب طلب رمز جديد

### خطأ في Firestore؟
- تأكد من إضافة القواعد في `firestore.rules`
- نشّر القواعد الجديدة:
```bash
firebase deploy --only firestore:rules
```

---

## 📞 الدعم

إذا واجهت مشاكل:
1. راجع logs في VS Code
2. تحقق من console في لوحة EmailJS
3. راجع Firestore في Firebase Console

---

تم التحديث: ديسمبر 2025 🚀
